package Combination.world.blocks;

import mindustry.world.meta.*;

public class CMStat {
    public static final Stat

    powerTarget = new Stat("powerTarget", StatCat.power),
    minLoad = new Stat("minLoad", StatCat.power),
    maxLoad = new Stat("maxLoad", StatCat.power);
}
